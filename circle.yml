machine:
  ruby:
    version: "2.2.3"

dependencies:
  post:
    - bundle exec ruby app.rb: # will launch the web server on the port 4567
        background: true

test:
  override:
    - bundle exec rspec spec/data_test.rb
